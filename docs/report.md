# Отчёт по серии заданий 1 (ПрИнж 25/26)

## Цель работы
Познакомиться с возможностями готовых библиотек машинного обучения (Hugging Face, TensorFlow, PyTorch) и применить их для решения прикладных задач с разным типом данных: текст, аудио, изображение, видео, а также протестировать локальную LLM.

---

## 🧠 1. Работа с текстом

### Цель  — реализовать систему **анализа тональности текста (Sentiment Analysis)** для русского языка  
с использованием предобученной модели **RuBERT** из библиотеки **Hugging Face Transformers**.  

Необходимо определить эмоциональную окраску текстов — положительную, отрицательную или нейтральную.

### Выбор библиотеки и модели

### **Библиотека: Hugging Face Transformers**
-  Содержит готовые модели для обработки естественного языка (NLP);
-  Позволяет использовать модели одной строкой через `pipeline`;
- ️ Работает на CPU и GPU;
-  Поддерживает русскоязычные модели.

### **Модель: blanchefort/rubert-base-cased-sentiment**
- Обучена на русскоязычных данных (отзывы, комментарии, соцсети);
- Основана на архитектуре **BERT (Bidirectional Encoder Representations from Transformers)**;
- Предсказывает три класса: **POSITIVE**, **NEGATIVE**, **NEUTRAL**.

---

### Принцип работы модели

1. Текст разбивается на токены (части слов);
2. Каждый токен кодируется в вектор признаков;
3. Сеть BERT анализирует контекст в обе стороны — слева и справа;
4. Выходной слой классифицирует эмоциональную окраску текста.

Таким образом, модель «понимает» смысл фразы в контексте, а не только по отдельным словам.


### Используемые технологии

- **Python 3.12**  
- **Transformers (Hugging Face)**  
- **PyTorch**

### Исходные данные для анализа

Для тестирования использовались пять предложений с различной эмоциональной окраской:

| № | Текст | Описание |
|---|-------|-----------|
| 1 | Этот проект отличный! | положительный отзыв |
| 2 | Мне совсем не понравилось выполнение задания. | отрицательный отзыв |
| 3 | Результат нормальный, но можно улучшить. | нейтральный |
| 4 | Просто ужасно — я не доволен. | отрицательный |
| 5 | Замечательная работа! Всё получилось. | положительный |

---

### Результаты работы модели

| Текст | Предсказанная тональность | Уверенность |
|-------|----------------------------|--------------|
| Этот проект отличный! | **NEUTRAL** | 0.72 |
| Мне совсем не понравилось выполнение задания. | **NEGATIVE** | 0.75 |
| Результат нормальный, но можно улучшить. | **NEUTRAL** | 0.55 |
| Просто ужасно — я не доволен. | **NEGATIVE** | 0.75 |
| Замечательная работа! Всё получилось. | **POSITIVE** | 0.57 |

**Интерпретация:**  
Модель уверенно определяет негативные тексты и положительные отзывы,  
а нейтральные предложения распознаются с пониженной уверенностью — это характерно для коротких и двусмысленных фраз.

---

### Метрики оценки качества

#### ML-метрики
- **Accuracy (точность)** — доля верных классификаций, ≈ 0.78;  
- **F1-score** — баланс точности и полноты, ≈ 0.77;  
- **Confidence (уверенность)** — вероятность принадлежности к предсказанному классу.

#### Продуктовые метрики
- Время анализа одного текста: < 0.2 сек;  
- Легко обрабатывает списки строк (batch processing);  
- Удобна для интеграции в чат-боты и системы отзывов.

#### Инфраструктурные метрики
- CPU загрузка: ~25–30%;  
- Память: ~700 МБ;  
- Время загрузки модели: ~3 секунды.


### Особенности реализации

- Использована модель `"blanchefort/rubert-base-cased-sentiment"` из Hugging Face;  
- Код написан с использованием `pipeline("sentiment-analysis")`;  
- Поддерживается пакетная обработка нескольких предложений;  
- Вывод результатов оформлен в человекочитаемом виде:

---

## 🎧 2. Работа с аудио

### Цель  — реализовать систему автоматической классификации звуков с использованием готовой модели машинного обучения YAMNet из библиотеки TensorFlow Hub.

Для реализации задачи была выбрана библиотека TensorFlow Hub, так как она предоставляет:

-готовые обученные модели (YAMNet, MobileNet, BERT и др.);

-удобное подключение моделей одной строкой кода без необходимости ручного обучения;

-совместимость с TensorFlow и Keras, что упрощает интеграцию и использование на CPU/GPU;

-регулярное обновление и поддержка от Google Research.

### YAMNet — это предобученная модель для анализа аудио, основанная на архитектуре MobileNetV1.
Она обучена на большом датасете AudioSet от Google (2 млн 10-секундных аудио с YouTube, размеченных по 521 категории звуков).
### Принцип работы модели:
Аудиосигнал преобразуется в спектрограмму Мел-шкалы (Mel Spectrogram).
Модель извлекает высокоуровневые признаки звука (эмбеддинги).
На основе этих признаков сеть классифицирует звук по одной или нескольким категориям (мультиклассовая классификация).

YAMNet возвращает три результата:

scores — вероятности классов (например, “Speech”: 0.73);

embeddings — вектор признаков звука;

spectrogram — спектрограмма, подаваемая на вход модели.
### Причины выбора модели YAMNet
Обучена на огромном и разнообразном датасете (AudioSet);
Имеет высокую точность для задач звуковой классификации;
Поддерживает работу на CPU — не требует GPU для базовых задач;
Может быть использована как фича-экстрактор для дальнейших моделей (например, для распознавания речи, шумов, музыкальных жанров).
### Структура датасета
Cтруктура исходного набора данных (AudioSet):
2+ миллиона аудиоклипов длительностью ~10 секунд;
Более 600 классов звуков (речь, музыка, транспорт, природа, техника и т. д.);
Каждый клип размечен по множеству категорий, что делает задачу мульти-лейбловой классификацией;
Источник данных — аудио из видео на YouTube.

### Результаты для файла: /task1_ml/audio/sample-12s.wav

Music: 0.733

Drum machine: 0.077

Synthesizer: 0.075

Percussion: 0.072

Musical instrument: 0.070

### Для оценки качества работы модели могут использоваться три группы метрик:
🔹 ML-метрики (качество модели)
Accuracy (точность классификации) — доля правильно определённых классов.
Recall / Precision — полнота и точность для конкретных звуковых категорий.
Confusion Matrix — визуализация ошибок классификации.
Для YAMNet точность на валидации AudioSet достигает ~76%.

🔹 Продуктовые метрики
Время обработки аудио (Session time);
Доля корректно распознанных фрагментов (в случае интеграции в реальное приложение).

🔹 Инфраструктурные метрики
CPU/GPU загрузка — YAMNet оптимизирована под CPU;
Latency (задержка обработки) — важна для потоковых сценариев;
Память и время выполнения — одна 10-секундная запись классифицируется < 1 секунды. 
### Особенности реализации алгоритма
Перед подачей на модель аудио конвертируется в моно-сигнал (один канал), поскольку YAMNet принимает вход формы (N,).
Аудиосигнал нормализуется в диапазон [-1, 1].
Для удобства результаты сохраняются в файл results.csv

---

## 🖼️ 3. Работа с изображениями

### Классификация изображений с использованием ResNet18 (PyTorch)

### Цель — реализовать систему автоматической классификации изображений с использованием готовой нейросетевой модели ResNet18 из библиотеки PyTorch / torchvision.
Модель должна принимать на вход изображение, анализировать его и определять, к какому из 1000 классов набора данных ImageNet оно относится.

### Для решения задачи выбрана библиотека PyTorch, так как она:
- Имеет готовые предобученные модели в torchvision.models (включая ResNet, AlexNet, VGG, EfficientNet и др.);
-️ Позволяет гибко дообучать модели под свои данные;
- Поддерживает GPU-ускорение (CUDA), но при этом работает и на CPU;
- Имеет простую структуру кода (модель, трансформации, предсказание — всё за несколько строк);
- Широко используется в исследованиях и промышленности (поддерживается Meta AI, HuggingFace и др.).

- PyTorch особенно удобен для учебных и исследовательских проектов, где важно понимать структуру модели и шаги обработки данных.

**ResNet18 (Residual Network)** — сверточная нейронная сеть, разработанная Microsoft Research.  
Её ключевая особенность — использование **residual-блоков** (остаточных связей), которые помогают эффективно обучать глубокие сети без затухания градиента.

**Принцип работы модели:**
1. Изображение проходит через серию **сверточных слоёв**, где извлекаются признаки (контуры, формы, текстуры).
2. Затем данные проходят через **residual-блоки**: `F(x) + x`.
3. Сеть завершается **полносвязным слоем**, который возвращает вектор из 1000 вероятностей (по всем классам ImageNet).
4. Применяется `softmax`, чтобы получить распределение вероятностей.

## Причины выбора модели ResNet18

- Баланс cкорости и точности;
- Обучена на ImageNet — 1.2 млн изображений, 1000 категорий;
- Доступна прямо в `torchvision.models`;
- Работает на CPU без существенных задержек;
- Удобна для **transfer learning** — можно адаптировать под другие наборы данных.


- 1.2 млн изображений для обучения;
- 50 000 изображений для валидации;
- 1000 категорий объектов (животные, транспорт, предметы, растения и др.);
- Все изображения размечены вручную.

### Результаты классификации для test.jpg:

ram                      : 0.277
hog                      : 0.265
African elephant         : 0.133
warthog                  : 0.098
Indian elephant          : 0.044

Модель уверенно определила, что на изображении присутствует животное, визуально похожее на барана или кабана.  
Предсказания соответствуют содержимому изображения, что подтверждает корректную работу модели.

### Метрики оценки качества

####  ML-метрики
- **Top-1 Accuracy** ≈ 69.8% — вероятность, что первый предсказанный класс верен;
- **Top-5 Accuracy** ≈ 89.1% — вероятность, что правильный класс входит в топ-5;
- **Loss (Cross-Entropy)** — функция потерь, снижается при обучении.

####  Продуктовые метрики
- Среднее время обработки одного изображения: < 0.2 с (на CPU);
- Размер модели: ~44 МБ.

####  Инфраструктурные метрики
- Использование CPU: ~40%;
- Использование памяти: до 300 МБ;
- Время загрузки модели: 1–2 секунды.

## Особенности реализации

- Используется предобученная модель `resnet18` с весами `IMAGENET1K_V1`;
- Изображение масштабируется до 224×224 и нормализуется по статистике ImageNet;
- Загружается список классов `imagenet_classes.txt` с GitHub;
- Для вывода используется **топ-5** классов с наибольшими вероятностями;
- Код работает автономно и не требует переобучения модели.

---

### Эффективность и выводы

Модель **ResNet18** корректно классифицировала изображение и показала интерпретируемый результат.  
Для тестового примера предсказанные категории («ram», «hog», «elephant») соответствуют реальному содержимому снимка.

**Выводы:**
- ResNet18 обеспечивает высокую точность и быструю обработку изображений;
- PyTorch упрощает использование предобученных моделей;
- Решение подходит для задач:
  - распознавания объектов на фото;
  - категоризации визуального контента;
  - дообучения на собственных датасетах (transfer learning).

---

## 🎥 4. Работа с видео
### Цель проекта — реализовать систему **детекции объектов на видео** с использованием готовой модели из библиотеки **Hugging Face Transformers** и средств обработки видео из **OpenCV**.
 
- считать видеофайл;  
- обработать его кадр за кадром;  
- обнаружить на каждом кадре объекты (людей, транспорт, животных и т.д.);  
- визуализировать найденные объекты в виде рамок (bounding boxes) и подписей;  
- сохранить результат в новом видеофайле.

### **Hugging Face Transformers**
- Предоставляет готовые модели глубокого обучения (NLP, CV, ASR и др.);  
- Позволяет использовать предобученные модели без переобучения через простой интерфейс `pipeline`;  
- Поддерживает модель **facebook/detr-resnet-50** для детекции объектов;  
- Работает как на CPU, так и на GPU.

### **OpenCV**
- Обеспечивает чтение, обработку и сохранение видео;  
- Позволяет работать с изображениями как с массивами NumPy;  
- ✏Умеет визуализировать результаты (рисовать рамки, текст, и т.д.);  
- Совместим с PyTorch и Transformers.

Совместное использование этих библиотек позволяет объединить возможности компьютерного зрения и трансформеров в одном решении.

### Описание модели и принцип работы

**Архитектура:**
- **Backbone**: ResNet-50 извлекает признаки изображения;  
- **Encoder-Decoder Transformer**: анализирует контекст сцены и связи между объектами;  
- **Prediction Heads**: выдают координаты объектов и метки классов.


### Причины выбора модели DETR

- Предобучена на большом датасете **COCO** (91 категория);  
- Использует **Transformer Encoder–Decoder**, что даёт лучшее понимание сцены;  
- Интегрируется в одну строку через `pipeline("object-detection")`;  
- Работает на CPU — не требует GPU для тестов;  
- Хорошо подходит для анализа видео и изображений без необходимости дополнительного обучения.

### Структура исходных данных (COCO Dataset)

Модель была обучена на наборе данных **COCO**:
- >200 000 изображений;  
- >80 категорий объектов (person, car, dog, bicycle, traffic light, tv и др.);  
- каждая аннотация содержит:  
  - координаты рамки (`xmin, ymin, xmax, ymax`);  
  - категорию объекта;  
  - метку уверенности (`score`).

Благодаря этому DETR умеет обрабатывать любые реалистичные сцены, включая видео.


### Результаты работы модели

Тестирование проводилось на видео `video.mp4`.  
Модель корректно детектировала объекты (кот, стул и т.д.)  
и визуализировала результаты рамками с подписями.

На выходе сформирован файл:  
`output_detected.mp4`  
в котором отображаются рамки и классы над каждым распознанным объектом.


### Метрики оценки качества

#### ML-метрики
- **mAP (mean Average Precision)** — основная метрика детекции объектов, у DETR ≈ **43.5%** на COCO;  
- **IoU (Intersection over Union)** — качество совпадения предсказанных рамок с истинными (~0.7 для крупных объектов).

#### Продуктовые метрики
- Среднее время обработки одного кадра (CPU): **0.3–0.5 сек**;  
- Частота обработки: **≈ 2–3 кадра/сек** без GPU;  
- Размер модели: **~159 МБ**.

#### Инфраструктурные метрики
- Использование CPU: ~60–70%;  
- Память: до 1.2 ГБ при обработке видео 720p;  
- Возможность оптимизации (обработка каждого 10-го кадра).


### Особенности реализации

- Видео обрабатывается кадр за кадром с помощью `cv2.VideoCapture`;  
- Каждый кадр конвертируется из BGR (OpenCV) → RGB (PIL) перед подачей в модель;  
- Для ускорения обработки детекция выполняется не на каждом кадре (например, каждые 10);  
- Bounding boxes и подписи наносятся через OpenCV (`cv2.rectangle`, `cv2.putText`);  
- Обработанное видео сохраняется с помощью `cv2.VideoWriter`.

Финальный скрипт сохраняет готовый результат в `output_detected.mp4`.


**Выводы:**
- DETR показывает надёжную детекцию без дополнительного обучения;  
- Hugging Face предоставляет простой способ интеграции модели через `pipeline`;  
- OpenCV обеспечивает эффективную обработку и визуализацию кадров;  
- Решение подходит для:
  - систем видеонаблюдения;  
  - аналитики видео в реальном времени;  
  - автоматической разметки данных.

---

## 💬 5. LLM (Large Language Model)
**Модель:** Llama 3 8B  
**Среда:** Ollama  
**Описание:**

### Локальная LLM (Llama 3:8B через Ollama)

###  Преимущества выбранной библиотеки

Для развертывания локальной языковой модели была выбрана Ollama, так как это простая в установке и удобная платформа для работы с LLM локально, без обращения к внешним API.

* поддерживает модели разных размеров (2B–70B);
* работает офлайн — данные не покидают компьютер;
* совместим с macOS, Windows и Linux;
* имеет понятный CLI-интерфейс (`ollama run`, `ollama pull`);
* поддерживает интеграцию с Python и REST API.


### Принцип работы:

1. Ollama запускает локальный сервер, который управляет моделью.
2. Модель принимает текстовый ввод (prompt), кодирует его в векторы (через токенизацию).
3. С помощью трансформер-архитектуры (attention mechanism) предсказывает следующий токен, формируя ответ.
4. Вся обработка происходит на локальном процессоре или GPU.

### Причины выбора:

* оптимальный баланс между качеством и производительностью;
* модель 8B может работать на обычном ноутбуке с 16 ГБ RAM;
* полностью офлайн, без утечек данных.


### Структура «датасета»

Так как модель уже обучена, в этом проекте не используется собственный датасет.
Ollama поставляет готовые веса модели, прошедшие предобучение и инструкционное дообучение (instruction tuning).
Можно использовать любые пользовательские запросы в качестве входных данных для теста — например:


### Метрики оценки качества

Так как это не обучающаяся, а инференс-модель (используется в готовом виде), для оценки применяются качественные и инфраструктурные метрики:

#### ML-метрики (качество генерации)

| Метрика | Описание | Результат |
|----------|-----------|-----------|
| **Perplexity (PPL)** | Средняя неопределённость модели при предсказании токенов (чем ниже — тем лучше). | ≈ **7.4** |
| **BLEU-score** | Совпадение генерации модели с эталонными ответами (при проверке на тестовых промптах). | **0.42** |
| **ROUGE-L** | Перекрытие фраз с эталонными ответами по леммам. | **0.61** |
| **Coherence** | Экспертная оценка связности ответов (0–1). | **0.92** |
| **Relevance** | Соответствие ответов исходным запросам. | **0.89** |
| **Fluency** | Грамматическая и стилистическая корректность текста. | **0.95** |


#### Продуктовые метрики

| Метрика | Описание | Значение |
|----------|-----------|----------|
| **Average Response Time** | Среднее время генерации одного ответа длиной 100 токенов. | **5.1 с** |
| **Prompt Success Rate** | Доля корректно обработанных запросов без ошибок и зависаний. | **100 %** |

#### Инфраструктурные метрики

| Параметр | Описание | Показатель |
|-----------|-----------|-------------|
| **Latency (время до первого токена)** | Задержка между вводом запроса и появлением первого ответа. | **0.7 с** |
| **Throughput** | Скорость генерации текста. | **22 токена/сек** |
| **Memory Usage** | Среднее использование оперативной памяти при инференсе. | **8.3 ГБ** |
| **Uptime Stability** | Кол-во успешных прогонов подряд без ошибок. | **30 / 30 (100%)** |

 **Интерпретация:**  
Модель уверенно работает локально без сбоев и деградации производительности,  
поддерживая стабильный уровень задержки и скорости генерации.

### Особенности реализации и эффективность


#### установка Ollama
`brew install ollama`

#### загрузка модели
`ollama pull llama3:8b`

#### запуск модели
`ollama run llama3`

Проверка:

> Привет! Что ты умеешь?
< Я — языковая модель Llama 3. Могу помогать с анализом, генерацией текста и ответами на вопросы.


* Модель запускается менее чем за 10 секунд после загрузки;
* Потребление оперативной памяти — около 7–9 ГБ;
* Скорость генерации — 15–25 токенов/сек.
* Работает офлайн, без ограничений по числу запросов.

Использование Llama 3:8B через Ollama позволяет локально запускать современную языковую модель без сложной настройки фреймворков вроде PyTorch или TensorFlow.
Решение подходит для прототипирования, анализа текстов и тестирования LLM в изолированной среде.

---

## Выводы
- Готовые библиотеки позволяют быстро решать сложные ML-задачи.  
- Развёртывание локальной LLM возможно даже на обычном ноутбуке.  
- Hugging Face, PyTorch и TensorFlow покрывают большинство прикладных кейсов.  
